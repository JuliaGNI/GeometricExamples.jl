# Lotka-Volterra 2d (singular Lagrangian) with Radau IIA VPRK Methods

```julia
run_list(iodeproblem(timestep = Δt, timespan = (0.0, Δt * nt)), "Lotka-Volterra 2d (singular Lagrangian)", tableaus_vprk_radau())
```
